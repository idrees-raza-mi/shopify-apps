/**
 * Phase 3/4 will fill this in — for now this is just a placeholder so
 * the sidebar nav has a working target.
 */
export default function BuilderPage() {
  return (
    <div className="px-10 pt-10 pb-16">
      <h1 className="font-serif-display text-[34px] text-[#1a1a1a]">
        Template Builder
      </h1>
      <p className="mt-1 text-[13px] text-text-muted">
        SVG template upload and canvas configuration. Coming in Phase 3 &amp; 4.
      </p>
    </div>
  );
}
